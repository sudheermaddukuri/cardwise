[project]
name = "cardwise"
version = "0.1.0"
description = "Credit card offers and deals tracker"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.35.0",
    "pydantic>=2.7.0",
    "pandas>=2.2.0",
]

[tool.uv]
package = false
